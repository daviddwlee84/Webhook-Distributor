# Webhook Distributor configuration
# Edit and restart: systemctl --user restart webhook-distributor
WEBHOOK_HOST=0.0.0.0
WEBHOOK_PORT=3128

# Optional: space-separated webhook URLs (overrides webhooks.txt if set)
# WEBHOOK_URLS=https://discord.com/api/webhooks/xxx https://discord.com/api/webhooks/yyy
